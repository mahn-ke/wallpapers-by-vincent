############################################################
## DO NOT CHANGE - THIS IS AUTOGENERATED BY mahn-ke/repos ##
############################################################

name: Restore volumes to docker-compose.yml

on:
  workflow_dispatch:
    inputs:
      date:
        description: 'Either restore snapshot before this date (YYYY-MM-DDTHH:MM:SS format, optional)'
        required: false
        type: string
      tag:
        description: 'Or restore latest backup with this tag (optional)'
        required: false
        type: string

concurrency:
  group: restore-${{ github.ref }}
  cancel-in-progress: true

jobs:
  restore:
    uses: mahn-ke/.github/.github/workflows/template-restore.yml@main
    secrets: inherit
    with:
      date: ${{ inputs.date }}
      tag: ${{ inputs.tag }}
